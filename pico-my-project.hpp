#pragma once

#include <stdio.h>

const uint DATA_IN_PIN = 13;
const uint DATA_OUT_PIN = 14;
const uint SIDESET_PIN = 8;

const uint NUM_LEDS_TO_EMULATE = 2; // Important: PIO needs to be changed when this number is changed!